<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acercow.oneday">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".note.HomeActivity" />
        <activity android:name=".permissiongrant.PermissionGrantActivity" />
        <activity
            android:name=".wizard.WizardActivity"
            android:theme="@style/AppTheme.Translucent" />
        <activity android:name=".note.preview.PreviewNoteActivity" />
        <activity
            android:name=".note.edit.EditNoteActivity"
            android:label="@string/title_activity_edit_note"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/AppTheme.LightStatusBar" />
        <activity
            android:name=".note.preview.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>